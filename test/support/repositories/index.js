const { MockNewsRepository } = require('./news');
const { MockTagRepository } = require('./tag');
const { MockTopicRepository } = require('./topic');

module.exports = {
  MockNewsRepository,
  MockTagRepository,
  MockTopicRepository
}
